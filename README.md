# 🎾 nina_tennis

Система управления теннисными турнирами: сетки, расписание матчей, судейство в реальном времени, публичные табло и роли (админ / судья / зритель).

Монорепозиторий на npm workspaces: бэкенд (NestJS + MongoDB), фронтенд (Next.js) и общая библиотека типов и логики.

![License](https://img.shields.io/badge/license-MIT-blue)
![Node](https://img.shields.io/badge/node-%E2%89%A518-green)
![NestJS](https://img.shields.io/badge/NestJS-10-ea2845)
![Next.js](https://img.shields.io/badge/Next.js-15-black)
![React](https://img.shields.io/badge/React-19-61dafb)
![MongoDB](https://img.shields.io/badge/MongoDB-8-47a248)

---

## 📋 Содержание

- [Возможности](#-возможности)
- [Технологии](#-технологии)
- [Структура проекта](#-структура-проекта)
- [Требования](#-требования)
- [Быстрый старт](#-быстрый-старт)
- [Переменные окружения](#-переменные-окружения)
- [Команды](#-команды)
- [Роли и доступ](#-роли-и-доступ)
- [Система судей](#-система-судей)
- [Тестирование](#-тестирование)
- [Документация и поддержка](#-документация-и-поддержка)
- [Лицензия](#-лицензия)

---

## 🚀 Возможности

### Турниры и сетки
- **Олимпийская сетка** (single elimination) с посевом игроков (snake-seeding)
- Визуализация сетки с соединительными линиями, зумом и **фильтром по раундам**
- BYE-слоты для неполных сеток
- Автоматическое продвижение победителей

### Судейство (live)
- **Рабочее место судьи**: подсчёт очков по правилам тенниса (0/15/30/40, deuce/advantage, тай-брейк до 7)
- **Быстрые клавиши**: `1`/`2` — очко, `Z` — отмена, `F` — завершить
- **Автосохранение** состояния (восстановление после рефреша страницы)
- Настройка подачи 🎾 и стороны корта
- **Публичное табло** `/m/:matchId` с автообновлением каждые 4 секунды, полноэкранный режим для ТВ/планшета

### Расписание и участники
- **Календарь игр турнира** — месячная сетка, клик по дню → список матчей с кортом и временем
- **Страница участника** — статистика (победы/поражения/винрейт/сеты), история матчей, запланированные игры
- **Профиль пользователя** с редактированием имени/email/пароля

### Система судей (referees)
- Админ генерирует **ссылку-приглашение** → человек регистрируется/входит по ней → становится судьёй турнира
- Судья может судить **любой матч** своего турнира
- У каждого матча видно, кто судил (история `judgedBy`)
- Таблица судей в админке с inline-редактированием

### Безопасность
- JWT-авторизация, ролевая модель (admin / referee / user)
- `RolesGuard` на всех записывающих эндпоинтах
- Безопасный JWT-секрет (в продакшене обязателен `JWT_SECRET`)

### Дизайн
- Тёмная/светлая тема (next-themes, без мигания при загрузке)
- Дизайн-система: переиспользуемые компоненты (Button, Card, StatusBadge, ScorePill и др.)

---

## 🛠 Технологии

| Слой | Технологии |
|------|-----------|
| **Бэкенд** | NestJS 10, MongoDB, Mongoose 8, JWT (Passport), bcryptjs |
| **Фронтенд** | Next.js 15 (App Router), React 19, TailwindCSS 3, next-auth, @tanstack/react-query, next-themes |
| **Shared** | TypeScript, class-validator, class-transformer |
| **Инфраструктура** | npm workspaces (монорепо), concurrently |

---

## 📁 Структура проекта

```
nina_tennis/
├── apps/
│   ├── backend/              # NestJS API (порт 3101)
│   │   ├── src/
│   │   │   ├── modules/
│   │   │   │   ├── auth/         # авторизация, JWT, RolesGuard
│   │   │   │   ├── users/        # пользователи, профиль
│   │   │   │   └── tournaments/  # турниры, группы, матчи, игроки, судьи
│   │   │   └── seed.ts           # наполнение тестовыми данными
│   │   └── test/                 # e2e-тесты
│   └── frontend/             # Next.js (порт 3100)
│       ├── app/
│       │   ├── admin/            # админка (турниры, группы, судьи)
│       │   ├── tournaments/      # публичные турниры, сетки, расписание
│       │   ├── players/[id]/     # страница участника
│       │   ├── profile/          # профиль пользователя
│       │   ├── m/[matchId]/      # публичное табло матча
│       │   ├── judge/[matchId]/  # судейство (шорткат)
│       │   └── invite/[token]/   # приём приглашения судьи
│       └── components/
│           ├── BracketView.tsx   # визуализация сетки
│           ├── Scoreboard.tsx    # табло матча
│           ├── MatchCard.tsx     # карточка матча
│           └── ui/               # дизайн-система
├── libs/
│   └── shared/               # общие типы и логика
│       └── src/
│           ├── scoring.ts       # движок подсчёта очков
│           ├── seeding.ts       # алгоритм посева
│           └── models/          # интерфейсы (Match, Player, Tournament)
└── package.json              # корневые скрипты монорепо
```

---

## ✅ Требования

- **Node.js** ≥ 18 (тестировалось на v24)
- **MongoDB** ≥ 6.0 (локально на порту 27017 или MongoDB Atlas)
- npm ≥ 9

---

## ⚡ Быстрый старт

### 1. Установка зависимостей

Из корня репозитория:

```bash
npm run install:all
```

Это установит зависимости в корне, `apps/frontend`, `apps/backend` и `libs/shared`.

### 2. Настройка MongoDB

Убедитесь, что MongoDB запущена локально (`mongodb://localhost:27017`) или укажите `MONGO_URI` в `apps/backend/.env` на ваш Atlas-кластер.

### 3. Наполнение тестовыми данными (опционально)

```bash
npm run seed
```

Создаёт тестового администратора и демо-данные. ⚠️ Скачивает аватары (требуется интернет, 1–2 минуты).

**Учётная запись админа по умолчанию:**
- Первый зарегистрированный пользователь автоматически становится администратором.

### 4. Запуск в режиме разработки

```bash
npm run dev
```

Запускает бэкенд и фронтенд одновременно (через `concurrently`):
- **Фронтенд:** http://localhost:3100
- **Бэкенд (API):** http://localhost:3101

Откройте http://localhost:3100 в браузере.

---

## 🔐 Переменные окружения

### `apps/backend/.env`

| Переменная | По умолчанию | Описание |
|-----------|--------------|----------|
| `MONGO_URI` | `mongodb://localhost:27017/projectdb` | Строка подключения к MongoDB |
| `JWT_SECRET` | — | Секрет для подписи JWT. **В продакшене обязателен**, иначе бэкенд упадёт с ошибкой. В dev используется небезопасный заглушка с предупреждением. |
| `PORT` | `3101` | Порт бэкенда (задаётся в `start:dev` скрипте) |

### `apps/frontend/.env.local`

| Переменная | По умолчанию | Описание |
|-----------|--------------|----------|
| `NEXT_PUBLIC_API_URL` | `http://localhost:3101` | URL бэкенда для клиентских запросов |
| `NEXTAUTH_URL` | — | Базовый URL приложения для next-auth |
| `NEXTAUTH_SECRET` | — | Секрет для next-auth (сгенерируйте: `openssl rand -base64 32`) |

---

## 📜 Команды

Все команды выполняются из **корня** репозитория, если не указано иное.

### Разработка

| Команда | Описание |
|---------|----------|
| `npm run dev` | Запуск бэкенда + фронтенда одновременно |
| `npm run dev:backend` | Только бэкенд (порт 3101, watch-режим) |
| `npm run dev:frontend` | Только фронтенд (порт 3100) |
| `npm run seed` | Наполнить БД тестовыми данными |

### Сборка

| Команда | Описание |
|---------|----------|
| `npm run build` | Полная сборка: shared → backend → frontend |
| `npm run build:shared` | Сборка общей библиотеки |
| `npm run build:backend` | Сборка бэкенда (NestJS) |
| `npm run build:frontend` | Сборка фронтенда (Next.js) |

### Установка

| Команда | Описание |
|---------|----------|
| `npm run install:all` | Установить зависимости во всех пакетах монорепо |

---

## 👥 Роли и доступ

Система использует три роли пользователей:

| Роль | Описание | Доступ |
|------|----------|--------|
| **admin** | Администратор | Полный доступ: создание/редактирование турниров, групп, игроков, управление судьями, судейство любых матчей |
| **referee** | Судья | Судейство матчей турнира, к которому привязан. Доступ к рабочему месту судьи |
| **user** | Обычный пользователь | Просмотр публичных страниц, редактирование своего профиля |

**Логика ролей:**
- Первый зарегистрированный пользователь автоматически становится `admin`
- Роль `referee` присваивается при принятии приглашения от админа турнира
- Проверка ролей на бэкенде через `@Roles('admin')` / `@Roles('admin', 'referee')` декораторы + `RolesGuard`

---

## ⚖️ Система судей

### Как это работает

1. **Админ** открывает страницу турнира → «Судьи» → нажимает «Создать ссылку»
2. Получает многоразовую ссылку-приглашение вида `/invite/<token>`
3. Отправляет ссылку будущим судьям
4. **Судья** переходит по ссылке, регистрируется или входит → автоматически становится судьёй турнира (роль `referee`)
5. Судья открывает любой матч турнира → «Судить» → ведёт счёт
6. При сохранении судья фиксируется в матче (`refereeId` + история `judgedBy`)
7. Имя судьи отображается в карточке матча и расписании

### Проверка прав

Судья может судить **только матчи своего турнира**. Бэкенд проверяет: матч → группа → турнир → `tournament.referees.includes(userId)`. Чужому турниру — отказ (403).

---

## 🧪 Тестирование

### Unit-тесты (логика скоринга и посева)

```bash
cd libs/shared && npm test
```

Покрывают:
- Движок подсчёта очков (геймы, сеты, тай-брейк, deuce/advantage)
- Алгоритм посева (snake-seeding, нормализация, конфликты)

### E2E-тесты бэкенда (авторизация)

> ⚠️ Требуется запущенная MongoDB.

```bash
npm run test:e2e:backend
```

Проверяют регистрацию, логин, JWT-профиль, ролевую модель.

---

## 📌 Примечания

- **Порты по умолчанию:** фронтенд `3100`, бэкенд `3101`, MongoDB `27017`
- **Polling для live-обновлений:** публичное табло опрашивает бэкенд каждые 4 секунды (WebSocket не используется — проще для деплоя)
- **Формат счёта:** строки вида `6-4 3-6 7-6(5)` (сет-сет, тай-брейк в скобках)
- **Полный снимок состояния** матча хранится в БД (`scoringState` + `pointHistory`) — позволяет точно восстановить судейство после рефреша

---

## 📚 Документация и поддержка

### Документация

| Документ | Описание |
|----------|----------|
| [CONTRIBUTING.md](.gitverse/CONTRIBUTING.md) | Руководство для контрибьюторов: как предлагать изменения, стиль кода, ревью |
| [CODE_OF_CONDUCT.md](.gitverse/CODE_OF_CONDUCT.md) | Кодекс поведения участников сообщества |
| [SECURITY.md](.gitverse/SECURITY.md) | Политика безопасности: как сообщать об уязвимостях |
| [LICENSE](LICENSE) | Лицензия MIT — условия использования и распространения |

### FAQ

**Как стать судьёй турнира?**
Попросите у администратора ссылку-приглашение. Перейдите по ней, зарегистрируйтесь или войдите — вы автоматически станете судьёй турнира. Подробности в разделе [Система судей](#-система-судей).

**Как запустить без интернета?**
`npm run seed` скачивает аватары игроков (нужен интернет). Для офлайн-разработки пропустите этот шаг — система работает и без тестовых данных, просто создайте турнир вручную через админку.

**Счёт не сохраняется / зритель не видит обновления?**
Судейство автосохраняется в БД с задержкой ~1.2с. Публичное табло обновляется каждые 4с. Убедитесь, что MongoDB запущена и бэкенд доступен (http://localhost:3101).

**Забыли пароль админа?**
Удалите коллекцию `users` в MongoDB и перерегистрируйтесь — первый пользователь снова станет админом.

### Контакты и каналы поддержки

- **Баг-репорты и фичи:** [GitHub Issues](../../issues)
- **Вопросы и обсуждения:** [GitHub Discussions](../../discussions) (если включены)
- **Уязвимости безопасности:** см. [SECURITY.md](.gitverse/SECURITY.md) — **не публикуйте** в публичных Issues
- **Автор:** [@petrenkonik](https://github.com/petrenkonik)

---

## 📄 Лицензия

Проект распространяется под лицензией [MIT](LICENSE).

```
MIT License — Copyright (c) 2026 petrenkonik
```

Свободно используйте, модифицируйте и распространяйте с сохранением уведомления об авторских правах.
